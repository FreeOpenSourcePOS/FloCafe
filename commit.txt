fix(products): resolve image upload bugs and add macOS edit menu

- Fix: Prevent ImageUploader buttons from submitting the product edit form
- Fix: Exempt GET /api/products/*/image from JWT auth to allow <img> tag rendering
- Fix: Conditionally render image thumbnails on products page to prevent 404s
- Fix: Add cache-busting (?t=timestamp) to image URLs to prevent browser caching of old images
- Fix: Allow deleting existing images by initializing ImageUploader with 'EXISTING' state
- Feat: Add fallback tile with product initials on products list page
- Feat: Add macOS system Edit menu in main process to enable Cmd+C/V shortcuts
- Perf: Optimize loadProductRelationsBatch to fix N+1 query
- Sec: Use named SQL parameters in products.ts update query
